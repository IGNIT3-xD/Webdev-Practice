enum ActiveStatus {
  ACTIVE
  BLCOKED
}

enum Role {
  USER
  ADMIN
  AUTHOR
}

enum PostStatus {
  Draft
  PUBLISHED
  ARCHEIVE
}

enum CommentStatus {
  APPROVED
  REJECTED
}

enum SubscriptionStatus {
  ACTIVE
  CANCELED
  EXPIRED
}
